import { StyleSheet } from "react-native";

export default function buttonStyle() {
    const styles = StyleSheet.create({
        container:{ padding: 10, alignItems: 'center', justifyContent: 'center', borderRadius: 5 }
    })

    return styles
}